apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - secret-database.yaml

secretGenerator:
  - name: backoffice-tls
    files:
      - ../../components/certs/tls.crt
      - ../../components/certs/tls.key
